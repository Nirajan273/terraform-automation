---
- name: Deploy Docker for the Team
  hosts: all
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600
#ee
    - name: Install Docker
      apt:
        name: 
          - docker.io
          - python3-pip
        state: present


